---
phase: 1
slug: ui-polish
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-23
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for Phase 1: UI Polish.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.
> Source: REQUIREMENTS.md, ROADMAP.md, codebase scan of ProcessTile.tsx, FilterBar.tsx, ClassificationPanel.tsx.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — raw Tailwind CSS |
| Preset | not applicable |
| Component library | none (no Radix, no shadcn) |
| Icon library | none — inline unicode/emoji (ⓘ ✏️ ×) |
| Font | system default (Tailwind base reset) — no custom font declared |

shadcn gate result: `components.json` not found. Project is React + Vite. However, per project context, this project uses raw Tailwind and no component library is to be introduced. shadcn initialization: **declined by project convention**. Registry safety gate: not applicable.

---

## Spacing Scale

Declared values (multiples of 4 only). Values are Tailwind class equivalents.

| Token | Value | Tailwind | Usage |
|-------|-------|----------|-------|
| xs | 4px | p-1 / gap-1 | Icon gaps, chip internal padding, status dot gap |
| sm | 8px | p-2 / gap-2 | Compact button padding, inline element gaps |
| md | 12px | p-3 / gap-3 | Tile internal padding (existing: `p-3`) |
| lg | 16px | p-4 / gap-4 | Modal internal padding (EDIT-03 comfortable padding) |
| xl | 20px | p-5 / gap-5 | FilterBar horizontal padding (existing: `px-5`) |
| 2xl | 24px | p-6 / gap-6 | Modal section separation |
| 3xl | 32px | p-8 | Reserved — not used in this phase |

Exceptions:
- Modal width: fixed `480px` (not on the 4-pt grid — dictated by EDIT-01 requirement: `~480px wide`)
- Hover popover: `pointer-events-none` — no clickable padding needed; `p-2` (8px) internal padding
- Textarea minimum height: 4 rows (EDIT-02) — height is row-count-driven, not pixel-driven
- VG micro-label (`vg`): 2px horizontal padding (`px-0.5`) acceptable as label is decorative

---

## Typography

| Role | Size | Weight | Line Height | Tailwind | Usage |
|------|------|--------|-------------|----------|-------|
| Body | 14px | 400 (regular) | 1.5 | `text-sm` | Tile process name (existing: `text-sm font-semibold` — name is exception), panel labels, general text |
| Label / micro | 10px | 500 (medium) | 1.2 | `text-[10px] font-medium` | VG chips (existing), VG micro-label (`vg`), filter status badges |
| Meta / muted | 12px | 400 (regular) | 1.4 | `text-xs` | Process ID + level line (TILE-01), brief description (existing), footer children count, section headings in panel |
| Heading | 14px | 600 (semibold) | 1.2 | `text-sm font-semibold` | Tile process name (existing), modal node name heading |

**Font weight vocabulary for this project: 400 (regular) and 600 (semibold) only.**

TILE-01 typography contract:
- Process ID and level line: `text-xs font-mono text-gray-500`
- Format: `{id} · L{level}` — e.g. `1.3.2.4 · L4`
- Monospace required (font-mono) to align dotted IDs visually across tiles
- Placed immediately below process name, above brief description if present

VG micro-label (`vg`) typography contract:
- `text-[10px] font-medium text-gray-500 uppercase tracking-wide`
- Placed inline before the chip group: `vg` followed by chip row

---

## Color

All values are Tailwind dark-theme colours already in use. No new colour values are introduced in this phase.

| Role | Tailwind Value | Usage |
|------|----------------|-------|
| Dominant (60%) | `bg-gray-900` (#111827) | Page background, FilterBar background (existing) |
| Secondary (30%) | `bg-gray-800` (#1f2937) | L1 tile background, modal/panel background (existing) |
| Surface deep | `bg-[#1a2030]` / `bg-[#151c28]` | L2 / L3+ tile backgrounds (existing) |
| Border / separator | `border-gray-700` | Panel borders, FilterBar bottom border, modal border (existing) |
| Input surface | `bg-gray-700` | Textarea and select backgrounds in ClassificationPanel (existing) |
| Accent (10%) | `blue-600` / `blue-400` / `blue-500` | Selected tile ring (`ring-blue-400`), Save button (`bg-blue-600`), focus borders (`focus:border-blue-500`) |
| Muted text | `text-gray-400` / `text-gray-500` | Secondary labels, placeholder text, ID/level meta line |
| Primary text | `text-white` | Process names, interactive button labels |
| Destructive | `red-700` / `red-500` | Descoped border (`border-red-500`), "Confirm Descope" button (`bg-red-700`), descoped status dot |

**Accent reserved for:** selected tile ring, Save classification button, focus state on form controls, selected filter toggle buttons. Not used for VG chips (VG chips use their own semantic colour palette below).

**VG chip colour palette (existing — do not modify):**

| Vertical Group | Background | Text | Abbreviation |
|---------------|------------|------|--------------|
| Fulfillment | `bg-blue-900` | `text-blue-300` | FUL |
| Assurance | `bg-amber-900` | `text-amber-300` | ASR |
| Billing | `bg-green-900` | `text-green-300` | BIL |
| Operations Readiness & Support | `bg-slate-700` | `text-slate-300` | ORS |
| Strategy Management | `bg-purple-900` | `text-purple-300` | SMT |
| Business Value Development | `bg-fuchsia-900` | `text-fuchsia-300` | BVD |
| Capability Management | `bg-teal-900` | `text-teal-300` | CAP |

**VG FilterBar toggle buttons (VG-01) must use the same chip colours.** Active state: chip background becomes the VG background with higher opacity. Inactive state: chip border uses VG text colour with `88` hex alpha, text uses VG text colour with `cc` alpha (matching the existing tag-filter pattern in FilterBar).

**Modal backdrop (EDIT-01):** `fixed inset-0 bg-black/50 z-40` — semi-transparent black overlay, click-to-close.

**Hover popover (TILE-02):**
- Background: `bg-gray-900 border border-gray-700`
- Text: `text-xs text-gray-300`
- Width: `max-w-[240px]`
- Position: `absolute bottom-full left-0 mb-1 z-50`
- `pointer-events-none` on popover container — non-negotiable (ⓘ and ✏️ must remain clickable)
- Show/hide: controlled by `hovered` state set after 250ms delay via `setTimeout`

---

## Component Contracts

### TILE-01 — Process ID + Level display

```
<p class="text-xs font-mono text-gray-500 mt-0.5">{node.id} · L{level}</p>
```

- Placed below process name `<p>`, before brief description
- Hidden on muted tiles (isMuted render path returns early — do not add)
- `mt-0.5` (2px) tight spacing — IDs are secondary metadata

### TILE-02 — Hover popover

```
{hovered && (
  <div class="absolute bottom-full left-0 mb-1 z-50 pointer-events-none
              bg-gray-900 border border-gray-700 rounded p-2 shadow-lg
              max-w-[240px] text-xs text-gray-300 leading-relaxed">
    {node.brief_description ?? node.name}
  </div>
)}
```

- Trigger: 250ms delay on `mouseEnter` via `setTimeout`; clear on `mouseLeave`
- `pointer-events-none` on wrapper — enforced, not optional
- ⓘ and ✏️ buttons must remain in DOM and clickable when popover is visible
- Falls back to `node.name` if `brief_description` is empty

### TILE-03 — VG chip micro-label + tooltip

```
<div class="flex items-center gap-1.5 mt-1.5">
  <span class="text-[10px] font-medium text-gray-500 uppercase tracking-wide">vg</span>
  <div class="flex flex-wrap gap-1">
    {chips with title={fullVGName}}
  </div>
</div>
```

- Each chip: add `title={vg}` attribute (full VG name as tooltip — native browser tooltip)
- `vg` micro-label is always present when there are VG chips; one label for the whole group

### VG-01 — FilterBar VG toggle buttons

- Section added after existing tag filters, separated by `<div class="w-px h-5 bg-gray-700 mx-1" />`
- Label before toggles: `<span class="text-xs text-gray-500 mr-1">VG:</span>`
- Toggle format matches existing category toggle pattern: `text-xs px-2.5 py-1 rounded border transition-colors`
- Active state: use VG chip background + text colour
- Inactive state: border uses VG text colour at `88` alpha, text at `cc` alpha

### VG-02 — Tree subsetting

- Filter logic: process is shown if ANY of its `vertical_groups` intersects with selected VG filters
- When no VG filters active: all processes shown (existing behaviour)
- Filter applied in the same filter pipeline as category/tag/team filters

### VG-03 — Active VG filter chips

- Match existing active-chip pattern: `text-xs bg-gray-700 text-gray-300 px-2 py-0.5 rounded cursor-pointer hover:bg-gray-600 flex items-center gap-1`
- Chip label: VG abbreviation (FUL, ASR, BIL, ORS, SMT, BVD, CAP)
- Remove icon: `<span class="text-gray-500">×</span>` (existing pattern)

### EDIT-01 — ClassificationPanel as centred modal

Current panel: `absolute z-50 top-0 left-full ml-2 w-72 ...`

Replace with:

```
{/* Backdrop */}
<div class="fixed inset-0 bg-black/50 z-40" onClick={onClose} />

{/* Modal */}
<div class="fixed inset-0 z-50 flex items-center justify-center pointer-events-none">
  <div class="pointer-events-auto bg-gray-800 border border-gray-700 rounded-lg
              w-[480px] max-h-[90vh] overflow-y-auto shadow-2xl p-6">
    ...content...
  </div>
</div>
```

- Width: `w-[480px]` — matches EDIT-01 `~480px wide` requirement
- `max-h-[90vh] overflow-y-auto` — handles long content without clipping
- `p-6` (24px) internal padding — EDIT-03 comfortable padding

### EDIT-02 — Textarea row minimums

Both textareas: `rows={4}` (currently `rows={2}`)
- Notes textarea: `rows={4}`
- Descope reason textarea: `rows={4}`

### EDIT-03 — Section separation

- Between sections: `mb-4` (existing `mb-3` → upgrade to `mb-4`)
- Section label style: `text-xs text-gray-400 mb-1.5 uppercase tracking-wide` (existing — retain)
- Visual divider between Classification section and Descope section: `<hr class="border-gray-700 my-4" />`

---

## Interaction States

| Component | State | Visual |
|-----------|-------|--------|
| Tile | Hover (0–250ms) | No change |
| Tile | Hover (250ms+) | Popover appears; ⓘ/✏️ remain visible and clickable |
| Tile | Selected | `ring-2 ring-blue-400 opacity-100` (existing) |
| Tile | Dimmed | `opacity-60` (existing) |
| Tile | Muted (VG filtered out) | `opacity-30 pointer-events-none` (existing muted path) |
| VG filter button | Inactive | Border + text in VG colour at partial alpha |
| VG filter button | Active | Solid VG chip background + text |
| Modal | Open | Backdrop + centred panel |
| Modal | Close | Click backdrop or existing close mechanism |
| Textarea | Focus | `focus:border-blue-500` (existing) |
| Descope button | Disabled | `opacity-50` (existing pattern) |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| TILE-01 ID line | `{id} · L{level}` — e.g. `1.3.2.4 · L4` (data-driven, not freeform copy) |
| TILE-02 popover | Process `brief_description` field content; fallback to process `name` if empty |
| TILE-03 micro-label | `vg` (lowercase, no punctuation) |
| VG-01 FilterBar label | `VG:` (before toggle buttons) |
| VG chip tooltip | Full VG name — e.g. `Fulfillment`, `Operations Readiness & Support` (native title attribute) |
| VG-03 active chip | Abbreviation only — FUL / ASR / BIL / ORS / SMT / BVD / CAP |
| Save button | `Save` (existing — retain) |
| Descope confirm button | `Confirm Descope` (existing — retain) |
| Descope cancel button | `Cancel` (existing — retain) |
| Notes placeholder | `Optional notes...` (existing — retain) |
| Descope reason placeholder | `Reason for descoping...` (existing — retain) |
| Empty VG filter result | No dedicated empty state needed — muted tiles remain visible at `opacity-30` when VG filter active (follow existing muted pattern) |
| Popover empty (no description) | Show process name as fallback — no "No description available" copy |

Destructive actions in this phase:
- **Mark Descoped**: two-step — click "Mark Descoped" button reveals form; type reason; click "Confirm Descope". No separate confirmation dialog. The `disabled` state on "Confirm Descope" while reason is empty serves as guard. (Existing pattern — retain.)

---

## Interaction Timing

| Interaction | Timing |
|-------------|--------|
| Hover popover appear | 250ms delay after `mouseenter` (TILE-02 requirement) |
| Hover popover disappear | Immediate on `mouseleave` (cancel pending timer) |
| Filter toggle response | Immediate (synchronous Zustand state update) |
| Modal open | Immediate |
| Save mutation feedback | Existing loading state via `updateClassification.isPending` |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized |
| Third-party | none | not applicable |

No new component libraries or registries introduced in this phase. All new UI is implemented with existing Tailwind utility classes and React state.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
